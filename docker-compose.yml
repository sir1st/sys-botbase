version: "2"

services:
  devkit:
    image: devkitpro/devkita64:latest
    volumes:
      - ".:/code"
    command: bash -c "cd /code && make -j8 && while true; do sleep 1; done"
    # command: bash -c "dkp-pacman -Syu --noconfirm && cd /code && make -j8 && while true; do sleep 1; done"